// All of these are functions
function ping(thisAlert) {
    thisAlert = "Pong"
    alert("Pong");
}

function serverIp(serverAddress) {
    serverAddress = "The server ip is BlockPro.serv.nu but the server is not even online right now.";
    alert(serverAddress);
}

function serverVersion(ServerVer) {
    serverVer = "The version(s) that the server is using is 1.15.2 - 1.16.5";
    alert(serverVer);
}



