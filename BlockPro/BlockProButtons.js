// All of these are functions
function ping(thisAlert) {
    thisAlert = "Pong"
    alert("Pong");
}

function serverIp(serverAddress) {
    serverAddress = "The server ip is BlockPro.serv.nu";
    alert(serverAddress);
}

function serverVersion(ServerVer) {
    serverVer = "The version(s) that the server is using is 1.15.2 - 1.16.4";
    alert(serverVer);
}



